#include <iostream>
using namespace std;
int a[10000] = { 0 };
int b[10000][3] = { 0 };
int bs[10000][3] = { 0 };
int main()
{
	int n, m;
	cin >> n >> m;
	for (int i = 1; i <= n; i++)
		cin >> a[i];
	for (int i = 1; i <= m; i++)
	{
		cin >> b[i][0] >> b[i][1] >> b[i][2];

	}
	for(int i=)
	return 0;
}