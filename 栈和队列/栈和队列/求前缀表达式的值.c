#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
	char s[35];
	int a[35];
	double ans = 0;
	gets(s);
	int len = strlen(s);
	for (int i = 0; i < len; i++)
	{
		if (isdigit(s[i]))
		{
			int *a=malloc(sizeof(int))
		}
	}
	return 0;
}